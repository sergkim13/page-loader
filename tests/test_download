from page_loader.page_loader import download
import requests_mock


def test_download():
    url = 'https://ru.hexlet.io/courses'
    file_path = download(url, '/tests/')
    m = requests_mock.Mock()
    m.get('fdfd', text)
    pass
